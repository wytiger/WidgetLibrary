<resources>

    <style name="AppDialogTheme" parent="android:Theme.Dialog">
        <!--除去title-->
        <item name="android:windowNoTitle">true</item>
        <!--除去背景色-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--除去边框-->
        <item name="android:windowFrame">@null</item>
    </style>

</resources>

